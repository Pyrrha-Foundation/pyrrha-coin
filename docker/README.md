# Pyrrha docker files

The 3 folders respectively contain:

- `current-stable`: docker files to build and publish the latest release of Pyrrha
- `gitlab-ci`: docker files to crate the docker image used in Pyrrha CI (deprecated, soon to be removed)
- `testing`: docker and configuration files to locally troubleshoot errors occurred while running nexa CI on gitlab.com
